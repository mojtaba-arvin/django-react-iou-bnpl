{
  "name": "shell",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "webpack serve --config webpack.config.js --port 3000",
    "build": "webpack --config webpack.config.js",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "@bnpl/shared": "workspace:*"
  },
  "devDependencies": {}
}
